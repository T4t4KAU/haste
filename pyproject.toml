[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "haste"
version = "0.1.0"
description = "Haste: High-performance Asynchronous Speculative Decoding Engine"
requires-python = ">=3.10"